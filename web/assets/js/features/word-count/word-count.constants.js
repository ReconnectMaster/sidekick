/**
 * Word count tool constants.
 */

/** Average reading speed used for reading-time estimate (words per minute). */
export const READING_SPEED_WPM = 200;

/** Unicode range for Thai script detection. */
export const THAI_CHAR_PATTERN = /[\u0E00-\u0E7F]/;
