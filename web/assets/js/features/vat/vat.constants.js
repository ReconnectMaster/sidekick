/**
 * VAT calculator preset rates and defaults.
 */

/** Quick-select VAT rate buttons shown in the UI. */
export const VAT_PRESET_RATES = [7, 10, 15, 20, 23];

/** Default VAT rate when the page loads (percent). */
export const VAT_DEFAULT_RATE = 20;
